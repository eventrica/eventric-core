name: Continuous Integration

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  call_shared_continuous_integration:
    uses: eventrica/.github/.github/workflows/continuous_integration.yml@main
    secrets: inherit